repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v4.5.0
  hooks:
  - id: check-yaml
  - id: end-of-file-fixer
  - id: trailing-whitespace
  - id: debug-statements

- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.14.8
  hooks:
  - id: ruff-check
    args: [--fix, --config, python-sdk/pyproject.toml]
    files: ^python-sdk/
  - id: ruff-format
    args: [--config, python-sdk/pyproject.toml]
    files: ^python-sdk/

- repo: local
  hooks:
  # --- PYTHON HOOKS ---
  - id: ty
    name: ty type checker
    entry: bash -c 'cd python-sdk && uvx ty check --exclude=examples'
    language: system
    types: [python]
    pass_filenames: false
    files: ^python-sdk/

  - id: pytest
    name: pytest
    entry: bash -c 'cd python-sdk && uv run pytest --ignore=tests/benchmarks --ignore=tests/integration'
    language: system
    pass_filenames: false
    files: ^python-sdk/

  # --- TYPESCRIPT HOOKS ---
  - id: ts-format
    name: format (ts-sdk)
    entry: bash -c 'cd ts-sdk && npm run format'
    language: system
    pass_filenames: false
    files: ^ts-sdk/

  - id: ts-lint
    name: lint (ts-sdk)
    entry: bash -c 'cd ts-sdk && npm run lint:fix'
    language: system
    pass_filenames: false
    files: ^ts-sdk/

  - id: ts-typecheck
    name: typecheck (ts-sdk)
    entry: bash -c 'cd ts-sdk && npm run typecheck'
    language: system
    pass_filenames: false
    files: ^ts-sdk/

  - id: ts-test
    name: test (ts-sdk)
    entry: bash -c 'cd ts-sdk && npm run test'
    language: system
    pass_filenames: false
    files: ^ts-sdk/