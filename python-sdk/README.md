## axon (Python SDK)

Composable wrapper around LLM calls with `before_call` / `after_call` hooks.

### Development

Run tests:

```bash
uv run pytest
```

Run the example:

```bash
uv run examples/simple.py
```
